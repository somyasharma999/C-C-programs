#include<stdio.h>
int main(){
	int a,b;
	printf("enter a : ");
	scanf("%d", &a);                //use %f if using float value
	printf("enter b : ");
	scanf("%d", &b);
	int sum = a+b;
	printf("sum is : %d", sum);
	return 0;
}
